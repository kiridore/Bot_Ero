# 更新日志

本文件记录 BotEro 的用户可见变更。版本号以 `core/config.py::BOTERO_VERSION` 为单一来源，遵循语义化版本；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

**维护约定**：每次用户可见变更 MUST 同 commit 新增版本节并 bump `BOTERO_VERSION`（新功能 minor / 修复 patch）；CHANGELOG 顶部 `[x.y.z]` 节必须与 `BOTERO_VERSION` 一致。纯文档/测试/内部重构可只记变更不 bump。

## [1.33.2] - 2026-09-01

### 修复

- **日程页选中按钮可读性**：选中态按钮（触发方式/星期/提醒范围）改为「浅绿底 + 深绿字」（`accent-soft` 底 + `accent-ink` 字，与全站配色配对一致），我的闹钟条目文字同步改深绿色——修复原近白字在淡绿底上看不清的问题

## [1.33.1] - 2026-09-01

### 修复

- **静态资产陈旧缓存**：`/static/`、`/shared/`、`/entries.json` 现返回 `Cache-Control: no-cache`（带 ETag 重验证）——修复导航改名（如闹钟→日程）后浏览器仍显示旧名称的问题

## [1.33.0] - 2026-09-01

### 新增

- **日程日历过滤与高亮**：日历新增过滤开关（隐藏循环闹钟 / 隐藏非我创建 / 隐藏群聊闹钟，勾选即时生效，默认隐藏循环闹钟）——作用于日历格子、+N 折叠与当日列表悬浮窗；「今天」格子与自己的闹钟条目高亮增强（强调色填充，一眼可辨）。底部闹钟列表不受过滤影响，始终显示自己的全部闹钟

## [1.32.0] - 2026-09-01

### 新增

- **日程日历页**：个人中心新增「日程」页（`/profile/schedule`），以月历展示自己与群内所有人的闹钟，重复闹钟自动展开填充；点日期快速添加，点闹钟查看详情（创建人/内容/规则），自己的闹钟可编辑（支持仅我/群内公开互转）与取消；网页新建闹钟可选「仅我 / 群内公开」。旧闹钟页 `/alarms` 重定向至新页，闹钟列表保留在新页底部

## [未发布]

- **CSS token 收编**：全站样式残留的硬编码颜色统一改为 `:root` token / `color-mix` 派生（内部重构，渲染不变，为主题机制铺路）


## [1.31.0] - 2026-08-31

### 新增

- **网页配色主题**：设置页新增「网页配色」分区，全站可在 报纸风（默认）/ 上班摸鱼 / 夜间模式 间切换——「上班摸鱼」为低饱和灰白风并低饱和灰化图片（保留少量色彩）；「夜间模式」为暖暗色调底、图片原样；选择保存在当前浏览器，更换设备不跟随，切换即时生效且刷新不闪白

## [1.30.0] - 2026-08-31

### 新增

- **排行榜与周打卡板油图片化**：`/排名`（`/rank`）与 `/本周板油` 指令输出改为图片卡片——含排名奖牌配色、成员圆形头像与称号展示；头像首次下载后写入本地缓存，后续下载失败自动回退缓存头像；图片渲染异常时回退原纯文本输出，不影响使用

## [1.29.1] - 2026-08-31

### 变更

- **数据备份日报精简**：每日 08:00 备份任务不再播报开场白、耗时与各项统计，只发一条「数据校验成功率：X%」；成功率不足 100% 时在消息前 @ 超管提醒；无打卡记录时视为 100%（不再每天误 @ 超管）

## [1.29.0] - 2026-08-31

### 新增

- **周报出版通知上线**：`BOTERO_WEEKLY_NOTIFY` 默认值从 `0` 改为 `1`——每周一 08:00 周报生成后，默认在群里发「第 N 期《小埃周报》已出版 + 链接」，并向社区时间线发一条新事件（actor 为小埃同学，卡片导读显示本周总消息数与总字数，「>>详情」跳转当期周报）；两类通知同开同关，置 `BOTERO_WEEKLY_NOTIFY=0` 可整体关闭

## [1.28.1] - 2026-08-31

### 变更

- **议事厅评论改为时间正序**：帖子详情页顶层评论从「最新在前」（id 倒序）改为「从旧到新」（id 正序）排列，翻页方向同步改为正序向后加载，与串内回复顺序一致

## [1.28.0] - 2026-08-31

### 新增

- **打卡时间线显示状态四态化**：个人设置的打卡可见性升级为按类型（私聊/网页打卡、群聊打卡）各自四选一——显示 / 模糊显示 / 仅打卡信息（不含图片）/ 完全隐藏；原「私聊打卡显示」「打卡图片清晰」两个开关自动迁移（前者关闭→隐藏，后者关闭→两类模糊），旧开关废除；「仅打卡信息」卡片对他人显示打卡文字并在右下角标注「图片仅作者可见」，作者本人任何状态下始终查看原图

## [1.27.0] - 2026-08-30

### 新增

- **模糊打卡卡片右下角注释**：作者开启「打卡图片对他人模糊」后，非作者查看时间线时卡片右下角显示「作者已开启图片模糊」小角注，说明模糊源自隐私设置而非图片损坏

## [1.26.1] - 2026-08-30

### 修复

- **活动**：创建/编辑拒绝纯空白标题（原可通过校验后 strip 存空串，公告/列表显示「」）
- **活动**：超管（SUPER_USER）在列表卡片与详情页可见「管理」入口（原先权限放行但界面无入口，需手输 URL）
- **活动**：修复列表卡片「管理」链接嵌套在外层锚点内导致 HTML 解析拆散卡片布局（成员列表逸出卡片框，e1c784b）

## [1.26.0] - 2026-08-30

### 新增

- 活动：Web 端「发起活动」页（`/activities/new`），创建成功生成可复制群公告文案
- 活动：Web 端活动管理页（`/activities/{id}/manage`，仅创建人/超管）：报名期编辑参数、开始（借道机器人心跳约 1 分钟生效并通知全员）、取消；进行期延长截止、提前结束
- 活动：活动列表/详情页对创建人显示管理入口

### 修复

- **时间线**：修复模糊打卡图点击穿透到原图（lightbox 遮罩层 pointer-events 未拦截，0eda894）

### 变更

- **修复 Windows 下跑团导出测试的编码失败**：`test_trpg_session` 导出用例读 meta.json/record.md 未显式指定 `encoding="utf-8"`，在 Windows 默认 GBK 环境抛 `UnicodeDecodeError`（源码读写本就 utf-8，纯测试侧修复；全量回归在 Windows 上首次全绿）
- **周报**：群更新周报生成流程固化为可复用 skill（开发工具内部变更，e58d8c3）
- **开发流程：多步任务计划先行固化**——新增 `specs/conventions.md` §开发计划先行 Constraint（多步开发任务动代码前 MUST 用 `superpowers:writing-plans` skill 写实施计划，存 `docs/superpowers/plans/`，单点小修豁免），`AGENTS.md` Toolchain reality 同步摘要

## [1.25.0] - 2026-08-26

### 新增

- **打卡隐私设置**：个人设置页新增两个开关——「私聊打卡显示在时间线上」（关闭后他人不可见，作者仍可见并带「仅自己可见」角标）与「打卡图片对他人清晰可见」（关闭后他人在时间线看到高斯模糊图）；私聊/网页打卡在打卡表与时间线事件携带私聊标记，历史数据不受影响；网页端打卡自此也上传时间线

### 变更

- **统一测试框架 pytest**：项目根 `pytest` 一键全量回归——`test/conftest.py` 在收集前把全部 `BOTERO_*` 数据路径重定向到会话临时目录（回归绝不触碰真实 `data.db`/`server_data`）；5 个脚本式 webapp 集成套件（论坛编辑/评论线程/投票、时间线未读、全站登录门控）迁至 `test/scripts/check_*.py` 由子进程包装器纳入（各自独立进程拿全新临时 DB，仍可单跑）；node 最小 DOM stub 渲染用例（含论坛列表/详情/新建/预填 4 个 DOM 用例）一并纳入常规回归，新增用例自动发现；跑团记录插件导出路径提取常量 `TRPG_RECORDS_ROOT`（行为不变），其测试不再写删真实 `server_data/`；触碰真实库的数据脚本 `db_oper`/`sort_image` 迁出 `test/` 至 `scripts/`；`test_llm.py`（真实调外部计费 API）排除出常规回归；修复 `test_trpg_char` 4 处过期断言（`/profile/trpg` → 当前 `WEB_TRPG_URL`，3.0 路径分区迁移遗留）；补 Windows 依赖 `tzdata`
- **共享样式文件更名**：`core/web/static/gallery.css` → `base.css`（引用路径 `/shared/gallery.css` → `/shared/base.css`）——该文件实为全站基础样式（报纸风 token 唯一来源 + 共享组件 + 全站滚动条），旧名易误解为图库页专属；仅命名与引用同步，行为不变

### 文档

- **README 同步当前架构**：已注册插件数 43 → 47、Web 功能模块 6/8 → 11（补时间线/议事厅/工具箱/周报/登录页分区），快速开始改用 `pip install -r requirements.txt`（旧三包示例缺 GitPython 等模块级依赖），补全站登录门控说明与 `scripts/botero.env` 单一来源；仅文档修正

## [1.24.5] - 2026-08-26

### 修复

- **谁是卧底退出房间崩溃**：等待阶段非房主退出时引用未赋值变量直接崩溃（用户无任何回复）；转移房主文案现仅在房主退出分支返回，普通成员退出正常提示

## [1.24.4] - 2026-08-26

### 修复

- **补卡权限与日期漏洞**：`/超级补卡`（免积分、免年度额度）与替他人补卡此前无任何权限校验，任何群员可白嫖或消耗他人积分；现两者均要求群管理员（与菜单及文档既有声明一致）。同时补卡目标必须为已结束的周/日——此前可对当前周或未来日期补卡，`remedy_week` 会预写未来 7 天打卡记录，白嫖连续打卡与周常

## [1.24.3] - 2026-08-26

### 修复

- **积分并发丢失更新**：`add_user_point` 改为单条原子 SQL 加减（原“读-改-写”在并发事件线程下互相覆盖，积分会凭空增多或丢失）；积分管理器新增 `spend` 条件原子扣减，抽奖与补卡付费路径改用它——余额校验与扣费一步完成，并发下不可能扣成负数；补充并发回归用例

## [1.24.2] - 2026-08-26

### 修复

- **时间线 XSS 漏洞**：主页时间线卡片对标题/描述先整体 HTML 转义再替换用户占位符——此前论坛帖子标题、工具箱等含用户原文的事件文案可携带 `<img onerror>` 类脚本在全员主页执行（存储型 XSS）；补充 DOM 渲染回归用例

## [1.24.1] - 2026-08-24

### 变更

- **议事厅浏览量展示改为图标**：帖子列表与详情页的「N 次浏览」文字改为与工具箱点击计数同款的自托管眼睛图标 + 数字，悬停提示「浏览次数」；两页引入共享图标库 `icons.js`

## [1.24.0] - 2026-08-24

### 新增

- **议事厅帖子浏览量**：打开帖子详情即在 meta 行显示「N 次浏览」，帖子列表同样展示；计数按详情请求次数累计（刷新重复计、作者自己计、编辑页预填加载也计，不按人去重），数据库 `forum_posts` 新增 `view_count` 列，存量库升级自动补列并从 0 起计

## [1.23.0] - 2026-08-24

### 变更

- **仙人彩奖池改为单一总池按比例派发**：旧制把当期投注机械切成 60/25/15 三个独立奖级池，某奖级被中走后独立滚存，会出现「二等奖中奖后的下一期奖金反而比三等奖少」的倒挂。新制下总奖池 = 当期投注 + 上期滚存（单一池），开奖按 4A → 3A → 2A 逐级派发，各奖级注单合计拿走「当时总池」的 40% / 15% / 5%（多注等比衰减后奖级内均分），池单调缩小且比例递减保证高奖级单注奖金恒大于低奖级；池浅不足均分时按下注先后各 1 分至耗尽；派发后剩余全部滚入下期总奖池。开奖播报与 `/仙人彩` 概况文案同步改为总池口径；数据库 `immortal_lottery_carry` 新增 `carry_total` 列，旧三列滚存自动合并迁移（旧列停用），生产升级无需手工操作

## [1.22.0] - 2026-08-24

### 新增

- **仙人彩注单与中奖明细展示**：`/仙人彩` 概况新增「本期注单」按人列出各自购买的全部号码；周日开奖播报同步列出注单，中奖发放明细从「奖级 号码 → 积分」升级为带中奖人（如 `· 小明 二等奖(3A) 6578 → 15 积分`）；周报有人命中大奖（≥3 位相同）时头版头条改为「仙人彩大奖落定」并新增「大奖得主」卡片（人名+号码），二版抽奖战报的仙人彩行升级为逐条中奖明细（人名/奖级/号码），旧期归档的计数格式前端向下兼容；顺带补上菜单中缺失的 `/仙人彩` 条目

## [1.21.3] - 2026-08-24

### 修复

- **周报页仅渲染头版，打卡/抽奖/语录/活跃/花絮板块全部缺失**：`weekly.js` 的 `renderCheckin()` 内引用了未定义变量 `d`（应为单独传入的抽奖数据），触发 ReferenceError 中断渲染——首个真实周报只剩期数/消息字数统计/仙人彩头条（该缺陷自周报上线即存在，此前从未渲染过带真实数据的周报）。现打卡与抽奖数据分别传入 `renderCheckin`，并新增周报页 node DOM 渲染回归（五板块、KPI 取值、活跃柱状图），自动纳入 pytest

## [1.21.2] - 2026-08-24

### 修复

- **周报定时生成从未生效**：`match()` 判定周一 08:00 心跳触发时已消费当分钟去重标记，`_handle()` 内再次查询 `should_run_on_heartbeat()` 恒为 False，定时分支实为死代码——上线首周两期周报均由"启动补漏"生成，掩盖了该缺陷；8-23 的日志覆盖校验拦下补漏后，8-24 08:00 结算未出报。现触发原因（定时/补漏）改由 `match()` 单次判定并经实例属性传入 `handle()`，两条路径均有独立回归用例

## [1.21.1] - 2026-08-23

### 修复

- **周报上线首周误出两期不完整周报**：周报 2026-08-16 上线当夜，"启动补漏"机制（进程启动后首个心跳即为上一周出报）在无任何数据校验的情况下，先后为消息日志尚不存在的 2026-08-03 周生成空白报、为仅含上线后约 14 小时消息的 2026-08-10 周生成残缺报，并占用第 1、2 期号。现在周报只为"消息日志最早记录早于周起点"的完整周生成——上线前的周不出报，清理脏数据后重启也不会补漏复活；新增 `scripts/purge_weekly_reports.py` 清理生产脏数据，清理后首个完整周（2026-08-17 起）将正确成为第 1 期

## [1.21.0] - 2026-08-22

### 优化

- **全站滚动条统一样式**：替换浏览器原生滚动条为报纸风直角细条（透明轨道、米色 6px 拇指，悬停加深、按下近墨色），页面滚动、时间线/周报侧栏、灯箱、评论区等所有滚动区域一致生效；Chromium 系走 `::-webkit-scrollbar`，Firefox 走标准 `scrollbar-color`/`scrollbar-width`

## [1.20.2] - 2026-08-22

### 修复

- **矮视口下左侧工具栏底部按钮不可点**：显示器缩放比例过大（视口 CSS 高度变小）时，时间线无限滚动使页面底部始终够不到，sticky 侧边栏（约 1100px 高）下半截永远进不了视口，「个人中心/工具箱/登录区/退出」无法点击。侧边栏现设视口高度上限并在内部滚动，任何视口高度下都完整可达；时间线/论坛/工具页共用此样式一并生效，周报页侧栏同步预防性加固；窄屏（<900px）侧栏为常规流不受影响

## [1.20.1] - 2026-08-22

### 修复

- **菜单周补卡费用文案**：`/菜单` 中周补卡标注「6点」改为「4点」，与实际扣费（`plugins/remedy_checkin` 中 `cost = 4`）一致

### 文档

- **开发文档全量同步**：`AGENTS.md` 重写为 AI 代理自足入口——新增文档地图（CLAUDE / KNOWLEDGE_BASE / kb / specs / CHANGELOG / roadmap / 部署文档索引）、webapp 要点（11 模块、全站登录门控、auth_deps 唯一副本、禁 `--workers`）、知识库维护分工表；修正全部失效事实（插件数 6→11、`register_plugin`/WS 配置/代理等 file:line 引用、「handle() 必须 try/except」改为框架层统一捕获的现行为）；`CLAUDE.md` 同步修正功能分区数、静态文件数（25→49）、路由清单（补 /forum /tools /weekly /login）、登录门控说明，核心模块表补 `config.py`/`auth.py`/`db/`/`onebot_client.py`/`title_defs.py`/`feature_packs.py`
- **路线图与部署文档同步**：`roadmap.md` 版本头 1.12.1 → 1.20.1，补 1.13–1.20 全部已交付里程碑；`docs/web-apps-deployment.md` 补全站登录门控说明与 `/login`、`/live`、`/tools` 路由行，验证命令改为未登录预期 302（原 200 判定已失效），环境变量表补 `BOTERO_FORUM_IMAGES_ROOT`/`BOTERO_FORUM_IMAGE_MAX_BYTES`，修复断开的 URL 表与重复章节号
- **规范文档同步**：`specs/architecture.md` 依赖树补全 `core/db/`（17 个业务 manager）、`core/web/`、`core/config.py`、`core/auth.py` 等模块，`plugin_pool` 代码段更新为含插件启用检查与框架层异常捕获的真实实现，入口点补 `scripts/botero.env` 环境注入步骤；`specs/conventions.md` 修正「无根级 requirements.txt」的失效声明（依赖清单已存在）、去掉重复的 print 禁令、硬编码常量章节注明 `BOTERO_*` 环境变量化现状；`specs/web-gallery.md` 两处静态资源清单补 `nav.js`/`lightbox.js`/`icons.js` 与 forum/tools/weekly 静态文件（共 49 个）；`specs/README.md` 更新 web-gallery 领域描述并注明 `llms.txt` 为上游文档镜像而非 spec
- **知识库全量同步**：修复 `KNOWLEDGE_BASE.md` 总索引的编辑残损行与模块数（10→11），目录树补 `scripts/`、`docs/`、`core/web/` 与 `nav.js`/`icons.js`；`kb/DATABASE.md` 补议事厅 7 表（多子投票/两级嵌套评论）、工具箱 4 表、`timeline_events`、`user_game_stats`（表数 20+ → 44+1）；`kb/OPERATIONS.md` 新增全站登录门控章节、版本表补 1.10–1.20；`kb/GAMEPLAY.md` 修正抽奖概率（0 积分 31%、普通 12%、稀有 5%、传说 4%）与卧底指令名；`kb/QUICK_REFERENCE.md` 批量刷新失效的 file:line 引用并补 `/周常`、`/称号一览`；`kb/PLUGIN_CATALOG.md` 修正 `/插件` 指令与功能包路径
- **提交规范新增分块要求**：一个 commit 只承载一个逻辑变更，无关改动（功能/修复/测试/文档）必须拆开提交；同一逻辑变更的配套文件（代码+测试+spec+菜单+CHANGELOG+KB）仍在同一 commit。`specs/conventions.md` 新增 §Commit 提交分块，`commit-msg` 钩子对单次暂存 >12 个文件输出分块提示（警告不阻断）
- **归档 superpowers 文档**：不再使用 superpowers skill 开发，将 `docs/superpowers/`（7 篇设计文档 + 10 篇实施计划）整体归档至 `docs/archive/superpowers/`，删除 `.superpowers/` 工作目录，并同步更新 CLAUDE.md / KNOWLEDGE_BASE.md / specs/timeline-protocol.md / docs/web-apps-deployment.md 中的引用路径（注：2026-08-30 起随「开发计划先行」流程重新启用 superpowers skills 与 docs/superpowers/plans/，见 [未发布]）

## [1.20.0] - 2026-08-22

### 新增

- **议事厅评论两级嵌套回复链**：评论可回复任意评论，形成 Twitter 式「顶层评论 + 缩进回复串」两级结构，回复的回复标注「回复 @某人」不再继续缩进；评论计数改为服务端总数
- **议事厅评论编辑**：作者可就地内联编辑自己的评论（textarea + 保存/取消），显示「已编辑」标记；编辑后时间线卡片按 `forum_comment:{id}` 同 key 重发（语义与帖子编辑一致，重新计未读）
- **议事厅评论删除语义升级**：删除仍有存活回复的评论改为软删占位（显示「该评论已删除」，回复链与子事件保留），无存活回复仍物理删除；回复目标不存在/跨帖/已软删时提交返回 400

## [1.19.1] - 2026-08-22

### 变更

- **称号文案调整**：称号「永远不死」描述改为「永生者」；称号「我还没有睡饱」更名为「睡觉」（描述「Zzzz」）；称号「人人网」更名为「板油」（描述「板油还不可以休息哦」）

## [1.19.0] - 2026-08-21

### 优化

- **时间线未读标记渐变褪回**：未读卡不再一直保持高亮——被看到（首次进入视口）后先停留约 0.8 秒，再以约 1.5 秒由高亮平滑渐变为正常样式；系统「减少动态效果」偏好下直接跳变。未读高亮降级为纯视觉提示：卡片渲染（加载）即上报已读回执，渐变动画与上报结果完全解耦、必定播放到结束

## [1.18.0] - 2026-08-21

### 新增

- **全站登录门控**：所有页面 / API / 图片媒体一律要求登录——未登录访问页面 302 重定向到登录页（`next` 参数记住来源，登录后跳回），直接调 API 或取图片返回 401；bot 时间线事件上报（独立事件令牌）与静态资源放行，不受影响
- **独立登录页 `/login`**：报纸风密钥登录页取代原页内弹窗主流程；`next` 仅接受站内相对路径（防开放重定向）；cookie 丢失但会话仍有效时进入登录页自动跳回（会话自愈）
- **登录态双通道**：服务端门控与依赖注入统一支持 `Authorization: Bearer` 头或根域 cookie `botero_key` 任一凭证（页面导航带不了 header，cookie 为页面门控凭据）
- **退出登录**：各页面用户卡片旁新增「退出」按钮，清除本地会话并回到登录页
- **页内会话失效统一出口**：`/shared/auth.js` 全局拦截同源 fetch 401，自动清会话并跳转登录页

## [1.17.0] - 2026-08-20

### 新增

- **时间线/议事厅图片灯箱**：时间线事件图片与议事厅帖子正文图片支持点击在当前页放大预览（Twitter 式灯箱），加载中有 spinner，ESC / 点击背景 / 右上角按钮均可关闭；新增共享组件 `/shared/lightbox.js`

## [1.16.1] - 2026-08-18

### 修复

- **匹配活动全员提交后不再提前结束**：此前所有成员提交完作品即自动结束归档，玩家失去反复覆盖修改的机会；现改为等待截止时间或创建人 `/活动 结束` 才归档（接龙不受影响，链走完仍即时结束）

## [1.16.0] - 2026-08-18

### 新增

- **时间线实时更新**：社区主页每 30 秒轻量轮询新事件，出现 Twitter 式「查看 N 条新事件」pill，点击后才把新卡置顶插入（不打断正在阅读的内容）；轮询 401 自动停止并提示重新登录
- **时间线逐卡未读/已读**：服务端按用户持久化未读状态（rowid 单调基线 + 逐事件已读回执），未读卡浅底高亮 + 左侧标线；卡片进入视口后完全离开才记为已读，刷新/换设备后仍准确；首访把既有历史视为已读；撤回事件自动从未读计数消失
- **时间线读状态 API**：`GET /api/timeline/poll`、`GET /api/timeline/new`、`POST /api/timeline/read`（鉴权同现有登录密钥）；事件响应新增 `seq`/`unread` 字段
- **forum 编辑语义变更**：编辑帖子重发的时间线事件按新事件重新入列并计算未读（撤回键仍为 `forum_post:{id}`，不变）
- 新增数据表 `timeline_user_watermarks` / `timeline_read_events`（启动自动建表，无需迁移）

## [1.15.0] - 2026-08-17

### 新增

- **议事厅投票单选/多选 + 多子投票**：一个投票帖可包含多个子投票，每个子投票各自设单选/多选——发布页新增子投票块（问题 + 单选/多选开关 + 动态增删选项），详情页按子投票分组渲染（单选点击即投、多选勾选后提交）；数据层新增 `forum_polls` 表，`forum_poll_options`/`forum_poll_votes` 改以子投票（`poll_id`）关联，旧库自动迁移（选项重挂子投票、投票重映射、唯一约束改为 `(poll_id, option_id, user_id)`），`forum_posts.poll_allow_multi` 废弃；投票 API 改为 `{poll_id, option_ids}`，单选一人一票、多选可投多个选项且同选项不重复

## [1.14.0] - 2026-08-16

### 新增

- **小埃周报**：每周一 08:00 自动出版一期报纸排版的群周报并永久归档——新系统插件 `message_logger` 将群消息记录到独立库 `server_data/message_log.db`，新定时插件 `weekly_report` 聚合消息日志与玩法数据（打卡/抽奖/仙人彩/周常/称号/活动/卧底），产出 5 版报纸数据（头条/群情数据/群友言论/群像观察/花絮）；webapp 新增第 11 个模块 `weekly`（`/weekly` 页面 + `/api/weekly` 归档 API）
- **抽奖流水**：`plugins/lottery` 每次抽奖后写 `lottery_draw_log`，供周报统计每周欧皇/非酋
- **配置新增**：`BOTERO_MESSAGE_LOG_DB_PATH`、`BOTERO_WEB_BASE_URL`、`BOTERO_WEEKLY_NOTIFY`

## [1.13.0] - 2026-08-14

### 新增

- **议事厅帖子编辑与删除**：帖子作者可在详情页编辑/删除自己的帖子——编辑复用发布页（`/forum/new?id=` 预填，类型与投票结构不可改，title/正文/tag 可改，保存后跳回详情页）；删除二次确认，级联删除评论/选项/投票/标签关联；编辑/删除同步维护时间线事件（编辑=撤回旧卡片并按同 key 重发最新内容，不产生新事件；删除=按 key 撤回），非本人操作返回 403
- **发新帖/编辑页回车防误触**：标题/tag 输入框按 Enter 不再触发表单隐式提交（需点「发布/保存修改」按钮）
- **编辑预填提速**：编辑模式立即拉取帖子原文填充标题/tag/正文，不再等待 Tiptap CDN（esm.sh）加载——CDN 慢/不可达时标题/tag 照常预填、正文待编辑器就绪后补填；CDN 导入 12s 超时快速降级（正文编辑器缺失时仍可保存标题/tag，正文保持不变）
- **悬空 tag 清理**：删帖/编辑移除 tag 后，引用数归零的 tag 不再展示「引用(0)」——tag 列表接口仅返回被引用 tag（count>0），删帖/改 tag 时同步删除悬空 tag
- **富文本渲染修复**：帖子正文的加粗/斜体/删除线/行内代码发布后正常显示——此前渲染端丢弃 text 节点的 marks，仅编辑器内可见，正文全部同字体

## [1.12.1] - 2026-08-13

### 修复

- **滚动揭示提速**：动态加载卡片（时间线/工具箱/活动等）不再滚到空白板面才出现——揭示观察器新增视口下方 25% 预揭示区（卡片在折叠线下即提前进场，滚动到时已完整可见），动态批次交错延迟从 55ms/级·上限 600ms 收紧为 40ms/级·上限 300ms（全站 `data-reveal` 生效）

## [1.12.0] - 2026-08-13

### 新增

- **时间线报头氛围动效**：社区主页报头分隔金线缓慢流动（9s 循环）+ 期号/标题/副题/日期依次浮现（90ms 级联）；`prefers-reduced-motion` 下全部关闭。独立提交，便于单独回滚

## [1.11.0] - 2026-08-13

### 新增

- **全站动效**：新增零依赖动效层 `core/web/static/motion.css` + `motion.js`（`/shared` 挂载，18 个页面统一注入；gallery.css `:root` 扩展动效 token `--motion-slow`/`--spring`/`--reveal-dist`）：
  - **MPA 整页平滑过渡**：View Transitions API（Chrome/Edge/Safari 生效，Firefox 自动降级普通跳转）
  - **卡片滚动进场揭示**：元素 `data-reveal` 标记即可，新增批次自动交错（55ms/级、上限 600ms，MutationObserver 接管，无需页面手动注册）；覆盖时间线/图库/议事厅帖子与评论/留言簿/闹钟/商店/称号/跑团/工具箱/活动卡片
  - **按压反馈**：按钮/导航/卡片链接按压缩放（触屏与 reduced-motion 自动跳过）
  - **图库 lightbox**：淡入淡出过渡（原 display 硬切），关闭动画中途重开已防护
  - **提示滑入**：全站 `settings-toast` 入场动画（display 恢复自动重播，无需改各页 showToast）
  - **数字滚动**：签到统计（本周收录/连续/最长周数）与商店积分从 0 滚动（easeOutCubic）
  - **投票反馈**：议事厅投票中「我投的选项」结果弹性弹入
  - **无障碍**：`prefers-reduced-motion: reduce` 一键关闭全部动效（motion.js 不注入、CSS 媒体查询兜底）；JS 失败时内容保持可见（渐进增强）
- 跑团/活动：webapp 无骰子交互且活动倒计时每秒跳动（不适合数字滚动），改由区块滚动揭示覆盖

## [1.10.0] - 2026-08-13

### 新增

- **工具箱**：webapp 新模块 `/tools`，仅登录用户可提交/编辑/删除自己的网页链接（标题/简介/URL/tag 均可修改，tag 整体替换），平铺展示为可点击卡片（icon 浏览器直连默认路径（走用户网络，恢复原有显示），失败或 10s 挂起时转服务端解析兜底——抓首页 `<link rel="icon">` 发现真实图标、结果入库缓存、无图标 404 负缓存 7 天、内网地址拒绝防 SSRF；加载失败/超时降级标题首字母；卡片展示提交者昵称与头像）；提交/删除自动推送/撤回时间线事件（昵称头像占位解析），修改后撤回旧事件并以同 key 重发最新内容；支持关键字搜索（标题/简介/URL）与卡片/列表双视图切换（偏好存 localStorage）；支持**点击统计**（卡片展示「N 次点击」，公开计数）与**双维度排序**（按添加时间/按热度 × 正序/倒序，默认时间+倒序与旧「最新」一致，偏好存 localStorage）；支持 **tag 系统**（提交时逗号分隔自由创建、每链接 ≤10 个每个 ≤20 字，卡片展示 tag 徽标，点击徽标按 tag 筛选，会话内可清除）；页面展示 **tag 云**（全部 tag 及使用数量，点击也可筛选，增删改后实时刷新）；头部操作与删除按钮图标化（添加/排序方向/视图切换/删除/编辑为内联 SVG 图标，点击次数以眼睛图标展示，图标自托管 lucide-static（ISC）无外链 CDN）；主页侧边栏与顶部站点栏新增入口
- **议事厅**：正文支持插入图片（工具栏 IMG 按钮上传本地 JPG/PNG/WebP/GIF 至服务器，≤10MB，插入 Tiptap 正文；详情页渲染图片，公开读取不可枚举）

## [1.9.1] - 2026-08-13

### 修复

- 时间线加载提速：昵称/头像解析改为并发（16 线程），首屏冷缓存耗时从 ~26s 降至 ~7s、热缓存 <30ms；前端分页 50 条/页收紧为 20 条/页瀑布流 + 预取距离 600px，数据量大时不再卡顿

## [1.9.0] - 2026-08-11

### 新增

- **社区时间线**：Event Server（`POST/DELETE /api/timeline/events`）+ 主页切换为时间线 + 侧边栏导航 + 无限滚动 feed + 登录门禁；打卡（含图片）、论坛发帖/评论/投票关闭自动推送事件；占位符解析昵称头像
- **议事厅**：长文/公告/投票/评论 + tag 管理 + Tiptap 富文本编辑器 + 到期自动关闭投票 + 新帖群通知
- **兑换码系统**：`/兑换码` 指令与注册表；一周年兑换码 `ONLY-YEAR-ONCE`（「1st」传说称号 + 10 积分）
- **打卡图片即时落盘**：打卡时立即下载图片，时间线/图库图片不再等次日备份任务
- 站点导航条新增议事厅入口；帖子/评论作者展示 QQ 昵称与头像

### 变更

- 周常任务完成不再推送时间线（`quest` source 停用，治理刷屏）
- 解锁称号不再推送时间线（`title` source 停用）
- Web 收敛为单进程：6 个独立子应用 → 单进程 webapp，子域改同源路径分区，导航主页并入，全站登录统一（`/shared/auth.js`）

### 修复

- 时间线打卡图 404：图片滞后备份任务（最长 12h）才落盘，改为打卡时即时下载
- 议事厅顶部导航栏样式缺失：页面未链接 `timeline.css`
- 帖子/评论显示裸 QQ 号：补作者昵称与头像解析
- 直播状态探测公网 hairpin 回环超时误报「未开播」

## [1.8.0]

- 单日补卡
- 更多积分消费（商店 buff：打卡增强/抽奖增强/额外抽卡/刷新次数）
- 称号/成就系统（抽奖、条件、游戏累计、兑换码多来源；商店货架只上架抽奖解锁称号）
- 发言统计插件与活跃称号（后移除）

## [1.7.0]

- 基于心跳包的定时插件系统（每天 08:00 自动备份打卡图片）
- 修复丢失图片

## [1.6.0]

- 积分兑换系统（积分商店）

## [1.5.0]

- 补卡功能（含每年次数上限）

## [1.4.0]

- 查看全部打卡图并自动生成合并转发
- 数据迁移：不再使用 QQ `get_image` 接口获取图片路径

## [1.3.0]

- 每周统计截止时间统一为周一早上 08:00
- 自动同意好友申请

## [1.2.0]

- 生成年度/月度打卡热力图
- 指令重启更新

## [1.1.0]

- 撤回打卡（含消息撤回联动）
- 查询服务器状态
- 打卡无需 @ 机器人

## [1.0.0]

- 打卡、打卡记录查询、历史打卡图查询、每周打卡板油查询

> 历史版本（≤1.8）内容摘录自 `kb/OPERATIONS.md` 版本历史表。
